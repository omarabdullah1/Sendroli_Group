{
  "build": {
    "dockerfilePath": "frontend/Dockerfile",
    "rootDirectory": "frontend",
    "buildCommand": "npm install && npm run build",
    "startCommand": "npm run preview"
  },
  "env": {
    "REACT_APP_API_URL": "https://sendroli-backend.up.railway.app"
  },
  "deploy": {
    "port": 3000
  }
}
