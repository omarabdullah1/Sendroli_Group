# Server Configuration
NODE_ENV=production
PORT=5000

# MongoDB Configuration
MONGODB_URI=mongodb://admin:nhPUGO8s0YIhqJPIyGhbklApO@mongodb:27017/factory_management?authSource=admin

# JWT Configuration
JWT_SECRET=change_this_to_a_very_secure_random_string_minimum_32_characters
JWT_EXPIRE=7d

# CORS Configuration
CORS_ORIGIN=https://sendroligroup.cloud

# Optional: Allow passwordless phone login globally (true/false). Defaults to false.
PASSWORDLESS_PHONE_LOGIN=true

# Security
BCRYPT_SALT_ROUNDS=12

# Logging
LOG_LEVEL=error

# Redis configuration for caching (optional). If not using Redis, NodeCache fallback will be used.
REDIS_HOST=redis
REDIS_PORT=6379
# REDIS_URL=redis://redis:6379

# Timeseries cache TTL in seconds (defaults to 300 if not set)
TIMESERIES_CACHE_TTL=300

# Dashboard cache TTL used by dashboard summary (seconds). Defaults to TIMESERIES_CACHE_TTL or 60s
# DASHBOARD_CACHE_TTL=300
